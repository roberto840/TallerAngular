import { BlackBackgroundDirective } from './black-background.directive';

describe('BlackBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
